rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    function isSignedIn() {
      return request.auth != null;
    }

    function isAdmin() {
      return isSignedIn() && exists(/databases/$(database)/documents/admins/$(request.auth.uid));
    }

    function isBootstrapAdmin(adminId) {
      return isSignedIn()
        && request.auth.uid == adminId
        && request.auth.token.email in ['Agrisahay@gmail.com', 'agrisahay@gmail.com'];
    }

    match /admins/{adminId} {
      allow read: if isAdmin() || (isSignedIn() && request.auth.uid == adminId);
      allow create, update: if isBootstrapAdmin(adminId) || isAdmin();
      allow delete: if false;
    }

    match /mechanics/{mechanicId} {
      allow read, create, update: if true;
      allow delete: if isAdmin();
    }

    match /jobs/{jobId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    match /communityPosts/{postId} {
      allow read: if true;
      allow create: if isAdmin();
      allow update, delete: if false;
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}