# AgriSahaya — Technician App

A pilot-scale (100–200 users) app for onboarding and managing agricultural-equipment
repair technicians: technicians sign up, get approved, and work jobs assigned by an
admin; admins review signups, manage the technician roster, and run the job board.

Design doc: [AgriSahaya Blueprint](https://claude.ai/code/artifact/698b31fa-d794-436c-99da-6ba65ea7ff0d)
(architecture, data model, and the reasoning behind the auth/job/notification design).

## Stack

- React + TypeScript, built with Vite
- [Capacitor](https://capacitorjs.com/) wraps the web app as a native Android app
- Firebase Authentication — phone OTP for technicians, email/password for admins
- Cloud Firestore — read-only from the client; every write goes through a Cloud Function
- Cloud Functions (v2) — owns all business logic (signup approval, job lifecycle,
  profile-update approval); Firestore rules only gate reads
- Firebase Cloud Messaging — job/notification pushes to the Android app
- Firebase Emulator Suite — local Auth/Firestore/Functions for development, no
  production data or SMS charges involved

The client never writes to Firestore directly (see `firestore.rules`) — it calls a
Cloud Function, which validates and writes via the Admin SDK. This is deliberate:
all business rules live in one place (`functions/src/`), not duplicated between
client and server.

## Repo layout

```
src/                   Web app (technician + admin UI)
  screens/             Add/assign jobs, community, profile-update review, technician jobs, etc.
  services/            Thin wrappers around Firestore reads + Cloud Function calls
  services/otp/        Phone OTP — real Firebase Phone Auth in prod, a local
                        random-code stand-in in the emulator (see below)
  i18n/                Technician-facing UI strings (en, hi, kn, ta, te, ml)
functions/             Cloud Functions backend (all writes happen here)
  src/lib/             Shared helpers (auth checks, doc lookups, validation, push)
  scripts/             create-admin.ts — one-off Admin SDK script, not deployed
android/               Generated by `npx cap add android` — gitignored, not committed
scripts/local-dev.mjs  One-command local stack: emulators + Android emulator + app
Makefile               Same local workflow as granular `make` targets (macOS/Linux)
firestore.rules         Read-gating rules (writes are all `if false`)
firestore.indexes.json  Composite indexes required by the queries in src/services/
```

## Prerequisites

- Node.js 22+ (`.nvmrc` provided — run `nvm use`)
- For the Android app: Android SDK (`ANDROID_HOME`) + a JDK (Android Studio's
  bundled JDK works) and an AVD (default `Pixel_6`, override with `AVD_NAME=...`)
- [Firebase CLI](https://firebase.google.com/docs/cli) (`npx firebase --version`
  works without a separate install — it's already a devDependency)

## Environment setup

Nothing is hardcoded — every environment (your machine, a teammate's, staging,
production) is configured entirely through env files, never source edits.

```bash
cp .env.example .env                     # frontend: Firebase Web SDK config
cp functions/.env.example functions/.env # backend: only needed for create-admin.ts
```

- **`.env`** (frontend) — the Firebase Web SDK config values from Firebase console
  → Project settings → General → Your apps → Web app. These identify which
  Firebase project the app talks to; they are not secrets, but are still
  env-driven so switching projects (e.g. a personal test project) never needs a
  code change. The app refuses to boot with a clear error if any are missing.
- **`functions/.env`** — only `GOOGLE_APPLICATION_CREDENTIALS`, used by the local
  `create-admin` script (see below). Deployed Cloud Functions get their
  credentials from the Cloud Functions runtime automatically and never read this
  file.
- **`.firebaserc`** — which Firebase project the `firebase` CLI targets
  (emulators, deploy). Already checked in; change it if you're pointing this repo
  at a different Firebase project.

## Run the web app

```bash
npm install
npm run typecheck
npm run dev       # Vite dev server, http://localhost:5173
```

Or in Docker, to avoid local Node/npm version drift:

```bash
docker compose up --build   # http://localhost:5173
```

## Run the Android app (Capacitor)

```bash
npm run build
npx cap sync android
npx cap open android
```

`android/` is generated, not committed — run `npx cap add android` once if it
doesn't exist. From Android Studio, build/run as usual, or from `android/` run
`./gradlew assembleDebug` for a debug APK.

## Run everything locally against the Firebase Emulator Suite

This is the full loop: Auth/Firestore/Functions emulators + the Android emulator +
the app, all on your own machine, no production Firebase traffic and no SMS
charges (phone OTP becomes a random 6-digit code shown on screen — see
[Local vs. production OTP](#local-vs-production-otp) below).

```bash
npm run dev:local     # macOS/Windows/Linux — plain Node.js
# or, on macOS/Linux:
make local
```

This one command detects your LAN IP, builds the web app against the right
Firebase-emulator host for whatever's connected (see below), syncs it into
the Android project (with cleartext HTTP enabled *only* for this build),
boots the Android emulator if nothing's connected, installs and launches the
app, builds the Cloud Functions, starts a browser-accessible Vite dev server
(so you don't have to run `npm run dev` separately, and don't accidentally hit
production Firebase from a browser instead of the emulator), and starts the
emulator suite bound to `0.0.0.0` so another device on the same Wi-Fi can
reach it too. Browser: http://localhost:5173. Emulator UI:
http://localhost:4000. Ctrl+C stops all of it.

**The AVD emulator and a real physical device need different hosts baked in**,
and this script picks the right one automatically: the AVD can *only* reach
this laptop via the special `10.0.2.2` alias — its virtual network can't
route to the laptop's own real LAN interface at all (confirmed: packets to
the LAN IP from inside the AVD come back corrupted, not just slow) — while an
actual phone on Wi-Fi has no such alias and needs the real LAN IP instead. If
you build with the wrong one, the symptom isn't a clear connection error —
the app just hangs forever on the "Loading..." screen, with
`auth/network-request-failed` buried in the console (`adb logcat`). If that
happens, this is the first thing to check.

If a physical device on Wi-Fi can't connect, check your OS firewall allows
inbound connections on ports 8080, 9099, 5001, 5173, and 4000.

### Granular `make` targets (macOS/Linux)

The same steps, runnable individually — useful when you only need to restart
one piece (e.g. just the emulator) instead of the whole loop:

| Target | What it does |
| --- | --- |
| `make sync` | Build the web app, sync into the Android project |
| `make emulator` | Boot the Android emulator if nothing's connected (clean boot, no snapshot) |
| `make emulator-reset` | Force-kill and reboot the emulator with a wiped data partition — use this if it's stuck |
| `make wait-emulator` | Boot and block until fully ready; self-heals (retries with a wipe) if boot stalls or crashes |
| `make doctor` | Report AVD/adb/emulator-log state without changing anything |
| `make install` | Build the debug APK and install it on the running device |
| `make launch` | Launch the installed app |
| `make run` | `install` + `launch` |
| `make local` | The full one-command loop (same as `npm run dev:local`) |
| `make logs` | Stream logcat for the running app |
| `make stop-emulator` | Kill the running emulator |
| `make clean` | Clean native Android build output |

The emulator always boots with `-no-snapshot`: the AVD's own snapshot cache has
been the single biggest source of a "boots fine once, then silently dies on the
next launch" failure, and this pilot's dev workflow never needs a snapshot's
saved state — Firestore is already reset to a fresh emulator each run.

### Local vs. production OTP

`src/services/otp/` picks between two interchangeable providers, controlled by
one existing flag (`VITE_FIREBASE_EMULATOR_HOST`, already set by
`dev:local`/`make local`, never in a normal build):

- **Production** — real Firebase Phone Authentication (`firebaseOtpProvider.ts`):
  sends an actual SMS.
- **Local/emulator** — `localOtpProvider.ts`: generates its own random 6-digit
  code (shown on screen, not sent anywhere) and signs in via a
  emulator-only `devSignIn` Cloud Function. That function is hard-guarded —
  it throws unless `FUNCTIONS_EMULATOR=true`, which the Firebase emulator sets
  automatically, so it can never run against production even if deployed by
  mistake.

## Firebase project setup (a real, non-emulator project)

Enable in Firebase console:

1. Authentication → Phone (for technicians) and Email/Password (for admins)
2. Firestore Database
3. Cloud Functions
4. Cloud Messaging (for job notifications)

Deploy the backend:

```bash
firebase deploy --only firestore:rules,firestore:indexes,functions
```

### Bootstrap the first admin

There's no in-app path to create an admin — `firestore.rules` denies every
client write to `/admins` on purpose. Use the one-off script instead:

```bash
cd functions
npm install
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json \
  npm run create-admin -- "admin@example.com" "a-strong-password" "Admin Name"
```

(Or put `GOOGLE_APPLICATION_CREDENTIALS` in `functions/.env` once instead of
prefixing every command — see `functions/.env.example`.) Get a service account
key from Firebase console → Project settings → Service accounts → Generate new
private key.

## User guide

For how the app works from a technician's or admin's point of view — signup,
approval, jobs, notifications, profile changes — see [USER_GUIDE.md](USER_GUIDE.md).

## Known gaps

- Push notification behavior (tagging a notification to a job, silently
  dismissing it when a job is cancelled/reassigned) is implemented but not yet
  verified on a real device while backgrounded/killed — see the comment at the
  top of `src/services/notifications.ts`.
- There is no PIN and no biometric app-lock — a technician signs in with
  phone-OTP only, and a persisted session opens the app directly. "Only one
  phone at a time" is enforced best-effort via refresh-token revocation
  (`revokeOtherSessions` in `functions/src/technicianFunctions.ts`): an old
  device's already-issued session can keep working for up to ~1hr after a new
  sign-in, since revocation doesn't invalidate an already-issued ID token.
- Kannada/Tamil/Telugu/Malayalam strings in `src/i18n/strings.ts` are a first
  pass, not yet reviewed by a native speaker (Hindi has been checked carefully).

## Requirement: Google Play Store release, multi-Android-version compatibility

The app needs to be publishable on the Google Play Store and run correctly across
the Android versions the project supports (`minSdkVersion` 24 through
`targetSdkVersion`/`compileSdkVersion` 36 — see `android/variables.gradle`). Any
dependency or native-config change made toward this must follow the process in
[docs/dependency_decison.md](docs/dependency_decison.md). See that evaluation's
findings for the current gap list before adding a release-signing, in-app-update,
or Play-Console-integration dependency.
