# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# Firebase emulator logs (generated by `npm run dev:local`)
firebase-debug.log
firestore-debug.log
ui-debug.log
.firebase/

# macOS
.DS_Store
*.pem

# Claude Code project instructions (local only)
CLAUDE.md

# local env files (see .env.example / functions/.env.example for the template)
.env
.env.*
!.env.example
functions/.env
functions/.env.*
!functions/.env.example
push-github-api.ps1

# typescript
*.tsbuildinfo

# Cloud Functions build output
functions/lib/

# generated native folders — mechanic app lives in /android (npx cap add android,
# default capacitor.config.ts), admin app in /android-admin (capacitor.admin.config.ts)
/ios
/android
/android-admin

/package-lock.json
/package.json
# Persisted Firebase emulator data (Auth users incl. password hashes, Firestore)
emulator-data/
emulator-data.bak/
keys/
